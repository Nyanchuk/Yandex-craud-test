// const runningText = document.getElementById('running-text');
// let text = runningText.textContent;
// let position = 0;

// function runText() {
//     runningText.textContent = text.substring(position) + text.substring(0, position);
//     position++;
//     if (position > text.length) {
//         position = 0;
//     }
//     requestAnimationFrame(runText);
// }

// requestAnimationFrame(runText);